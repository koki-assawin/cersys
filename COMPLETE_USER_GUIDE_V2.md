# 📘 คู่มือการใช้งานระบบเกียรติบัตร V2.0 (Google Drive Version)

## 🎯 ภาพรวมระบบ

ระบบเกียรติบัตร V2.0 ได้รับการปรับปรุงใหม่ให้รวดเร็วและใช้งานง่ายขึ้น:

### ✨ จุดเด่นใหม่:
- ⚡ **เร็วขึ้น 10 เท่า** - ไม่ต้องแยกไฟล์ PDF อีกต่อไป
- 🔗 **ใช้ Google Drive** - เก็บไฟล์บน Google Drive โดยตรง
- 🔐 **ระบบ Login** - แยกหน้า Admin และ User
- 📱 **ใช้งานง่าย** - UI สวยงาม ใช้งานสะดวก
- 🚀 **Deploy ได้** - พร้อมใช้งานบน Cloud

---

## 📋 สารบัญ

1. [การติดตั้งและเตรียมระบบ](#การติดตั้ง)
2. [การตั้งค่า Supabase](#ตั้งค่า-supabase)
3. [การเตรียมไฟล์ใน Google Drive](#เตรียม-google-drive)
4. [การใช้งานหน้า Admin](#ใช้งาน-admin)
5. [การใช้งานหน้า User](#ใช้งาน-user)
6. [คำถามที่พบบ่อย](#คำถามที่พบบ่อย)
7. [การแก้ปัญหา](#การแก้ปัญหา)

---

## 🔧 ส่วนที่ 1: การติดตั้งและเตรียมระบบ {#การติดตั้ง}

### ขั้นตอนที่ 1.1: ติดตั้ง Python

ถ้ายังไม่มี Python:
1. ดาวน์โหลดจาก: https://www.python.org/downloads/
2. เลือก Python 3.11 หรือ 3.12
3. **สำคัญ**: เลือก "Add Python to PATH"
4. ติดตั้งและรีสตาร์ท Command Prompt

ทดสอบ:
```bash
py --version
```

### ขั้นตอนที่ 1.2: ติดตั้ง Dependencies

```bash
# ไปที่โฟลเดอร์โปรเจค
cd C:\xampp\htdocs\cersys

# ติดตั้ง packages
py -m pip install -r requirements_v2.txt
```

---

## 🗄️ ส่วนที่ 2: การตั้งค่า Supabase {#ตั้งค่า-supabase}

### ขั้นตอนที่ 2.1: สร้าง Supabase Project

1. ไปที่: https://supabase.com
2. สมัครบัญชี (ฟรี)
3. คลิก "New Project"
4. กรอกข้อมูล:
   - **Name**: certificate-system-v2
   - **Database Password**: สร้างรหัสผ่านแข็งแรง (เก็บไว้)
   - **Region**: Singapore (ใกล้ไทยที่สุด)
5. คลิก "Create new project" (รอ 1-2 นาที)

### ขั้นตอนที่ 2.2: คัดลอก API Keys

1. ไปที่ **Settings** (เมนูซ้ายล่าง)
2. คลิก **API**
3. คัดลอกค่า 2 อย่าง:
   - **Project URL** (เช่น `https://xxxxx.supabase.co`)
   - **anon public** key (ตัวยาวๆ)
4. เก็บไว้ในที่ปลอดภัย

### ขั้นตอนที่ 2.3: สร้าง Database Tables

1. ไปที่ **SQL Editor** (เมนูซ้าย)
2. คลิก **"New Query"**
3. เปิดไฟล์ `setup_database_v2.sql` และ Copy ทั้งหมด
4. Paste ลงใน SQL Editor
5. คลิก **"Run"** (หรือกด F5)
6. ดูผลลัพธ์ว่าสำเร็จ

**ผลลัพธ์ที่ถูกต้อง:**
```
Database V2 setup completed successfully!
Created 1 admin users
Created 1 events
Default admin username: admin
Default admin password: admin123
```

### ขั้นตอนที่ 2.4: ตั้งค่า API Keys ในโปรเจค

สร้างไฟล์ `.streamlit/secrets.toml`:

```bash
# Windows
copy .streamlit\secrets.toml.example .streamlit\secrets.toml
```

แก้ไขไฟล์ `.streamlit/secrets.toml`:
```toml
SUPABASE_URL = "https://xxxxx.supabase.co"
SUPABASE_KEY = "eyJ..."
```
(ใส่ค่าที่คัดลอกมาจากขั้นตอน 2.2)

---

## 📂 ส่วนที่ 3: การเตรียมไฟล์ใน Google Drive {#เตรียม-google-drive}

### ขั้นตอนที่ 3.1: เตรียมไฟล์ PDF เกียรติบัตร

**สำคัญมาก:** ไฟล์ PDF ต้องถูกแยกออกเป็นไฟล์เดี่ยวแล้ว!

1. แยกไฟล์ PDF ให้เป็นไฟล์เดี่ยว (1 ไฟล์ = 1 คน)
2. ตั้งชื่อไฟล์เป็นตัวเลขตามลำดับ:
   ```
   1.pdf
   2.pdf
   3.pdf
   ...
   100.pdf
   ```

**เครื่องมือแนะนำสำหรับแยกไฟล์ PDF:**
- Adobe Acrobat DC
- PDFtk Free
- iLovePDF (ออนไลน์)
- Sejda PDF (ออนไลน์)

### ขั้นตอนที่ 3.2: อัพโหลดไฟล์ขึ้น Google Drive

1. เข้า Google Drive: https://drive.google.com
2. สร้างโฟลเดอร์ใหม่ เช่น "เกียรติบัตร_โครงการอบรม2026"
3. อัพโหลดไฟล์ PDF ทั้งหมดลงในโฟลเดอร์
4. ตรวจสอบว่าไฟล์เรียงลำดับถูกต้อง

### ขั้นตอนที่ 3.3: แชร์โฟลเดอร์

1. คลิกขวาที่โฟลเดอร์
2. เลือก **"Share"** (แชร์)
3. คลิก **"Change to anyone with the link"**
4. ตั้งเป็น **"Viewer"** (ดูได้อย่างเดียว)
5. คลิก **"Copy link"**
6. เก็บลิงค์ไว้

**ตัวอย่างลิงค์:**
```
https://drive.google.com/drive/folders/1a2B3c4D5e6F7g8H9i0J
```

### ขั้นตอนที่ 3.4: เตรียมไฟล์ Excel รายชื่อ

1. ดาวน์โหลด Template Excel จากระบบ (หน้า Admin)
2. กรอกข้อมูลตามคอลัมน์:

| ลำดับ | ชื่อ-นามสกุล | หมายเหตุ |
|-------|-------------|----------|
| 1 | สมชาย ใจดี | |
| 2 | สมหญิง รักดี | |
| 3 | ทดสอบ ระบบ | |

**หมายเหตุสำคัญ:**
- ลำดับใน Excel ต้องตรงกับชื่อไฟล์ PDF
- แถวที่ 1 = 1.pdf
- แถวที่ 2 = 2.pdf
- จำนวนแถวต้องเท่ากับจำนวนไฟล์ PDF

---

## 👨‍💼 ส่วนที่ 4: การใช้งานหน้า Admin {#ใช้งาน-admin}

### ขั้นตอนที่ 4.1: เข้าสู่ระบบ Admin

1. รันโปรแกรม Admin:
   ```bash
   py -m streamlit run admin_app.py
   ```
   หรือ double-click: `run_admin.bat`

2. เปิดเบราว์เซอร์ที่ http://localhost:8501

3. Login ด้วย:
   - **Username**: `admin`
   - **Password**: `admin123`

4. คลิก "เข้าสู่ระบบ"

### ขั้นตอนที่ 4.2: เปลี่ยนรหัสผ่าน Admin (แนะนำ)

**สำหรับความปลอดภัย ควรเปลี่ยนรหัสผ่านหลังใช้งานครั้งแรก**

1. ไปที่ Supabase Dashboard
2. เข้า SQL Editor
3. รันคำสั่ง:
```sql
-- เปลี่ยนรหัสผ่านเป็น "mypassword123"
-- (แทนที่ 'mypassword123' ด้วยรหัสผ่านที่ต้องการ)
UPDATE admin_users
SET password_hash = encode(digest('mypassword123', 'sha256'), 'hex')
WHERE username = 'admin';
```

### ขั้นตอนที่ 4.3: เพิ่มกิจกรรมใหม่

1. ในหน้า Admin คลิกเมนู **"➕ เพิ่มกิจกรรมใหม่"**

2. กรอกข้อมูล:
   - **ชื่อกิจกรรม**: เช่น "โครงการอบรม Python 2026"
   - **รายละเอียด**: รายละเอียดเพิ่มเติม (ถ้ามี)
   - **ลิงค์โฟลเดอร์ Google Drive**: Paste ลิงค์ที่คัดลอกมาจากขั้นตอน 3.3
   - **ไฟล์ Excel รายชื่อ**: อัพโหลดไฟล์ Excel

3. เลือกคอลัมน์ที่เป็น "ชื่อ-นามสกุล"

4. คลิก **"🚀 สร้างกิจกรรม"**

5. รอระบบประมวลผล (จะเร็วมาก ไม่กี่วินาที)

6. เมื่อเสร็จจะแสดงข้อความสำเร็จ ✅

### ขั้นตอนที่ 4.4: ดูรายการกิจกรรม

1. คลิกเมนู **"📋 จัดการกิจกรรม"**
2. จะเห็นรายการกิจกรรมทั้งหมด
3. คลิกที่กิจกรรมเพื่อดูรายละเอียด
4. สามารถลบกิจกรรมได้ (ระวัง: จะลบเกียรติบัตรทั้งหมดในกิจกรรมด้วย)

### ขั้นตอนที่ 4.5: ดาวน์โหลด Template Excel

1. คลิกเมนู **"📥 ดาวน์โหลด Template"**
2. คลิกปุ่ม **"📥 ดาวน์โหลด Template"**
3. เปิดไฟล์และกรอกข้อมูล
4. บันทึกไฟล์

---

## 👥 ส่วนที่ 5: การใช้งานหน้า User {#ใช้งาน-user}

### สำหรับผู้ใช้งานทั่วไป (ค้นหาและดาวน์โหลดเกียรติบัตร)

### ขั้นตอนที่ 5.1: เข้าใช้งานระบบ

1. รันโปรแกรม User:
   ```bash
   py -m streamlit run user_app.py
   ```
   หรือ double-click: `run_user.bat`

2. เปิดเบราว์เซอร์ที่ http://localhost:8501

3. จะเห็นหน้าค้นหาเกียรติบัตร (ไม่ต้อง Login)

### ขั้นตอนที่ 5.2: ค้นหาเกียรติบัตร

1. **เลือกกิจกรรม** จาก dropdown
2. **ใส่ชื่อ-นามสกุล** ในช่องค้นหา
   - ไม่ต้องใส่คำนำหน้า (นาย, นาง, นางสาว)
   - สามารถใส่เฉพาะชื่อหรือนามสกุลก็ได้
3. คลิก **"🔍 ค้นหา"**

### ขั้นตอนที่ 5.3: ดาวน์โหลดเกียรติบัตร

เมื่อพบเกียรติบัตร จะมีปุ่ม 2 ปุ่ม:

1. **📥 ดาวน์โหลด** - ดาวน์โหลดไฟล์ PDF
2. **👁️ ดูไฟล์** - เปิดดูไฟล์ใน Google Drive

---

## ❓ ส่วนที่ 6: คำถามที่พบบ่อย {#คำถามที่พบบ่อย}

### Q1: ระบบใช้เวลานานไหม?
**A:** ไม่! V2.0 เร็วมาก เพราะไม่ต้องแยกไฟล์ PDF แล้ว การเพิ่มกิจกรรมใช้เวลาแค่ไม่กี่วินาที

### Q2: ไฟล์ PDF ต้องแยกก่อนใช่ไหม?
**A:** ใช่ ต้องแยกไฟล์ PDF ให้เป็นไฟล์เดี่ยวก่อน (1 ไฟล์ = 1 คน) และตั้งชื่อเป็น 1.pdf, 2.pdf, ...

### Q3: ถ้าไฟล์ PDF มีหลายหน้า จะทำยังไง?
**A:** ต้องแยกออกเป็นไฟล์เดี่ยวก่อน ใช้เครื่องมือแยก PDF เช่น Adobe Acrobat, PDFtk, หรือเว็บออนไลน์

### Q4: จำนวนไฟล์ PDF กับรายชื่อใน Excel ต้องเท่ากันไหม?
**A:** ใช่ ต้องเท่ากัน และลำดับต้องตรงกัน (แถวที่ 1 = 1.pdf, แถวที่ 2 = 2.pdf)

### Q5: ต้องมี Google Drive API ไหม?
**A:** ไม่ต้อง เพียงแค่แชร์โฟลเดอร์เป็น "Anyone with the link" ก็ใช้ได้

### Q6: รหัสผ่าน Admin เริ่มต้นคืออะไร?
**A:** Username: `admin`, Password: `admin123` (ควรเปลี่ยนหลังใช้งานครั้งแรก)

### Q7: ถ้าลืมรหัสผ่าน Admin จะทำยังไง?
**A:** ไปที่ Supabase > SQL Editor แล้วรัน:
```sql
UPDATE admin_users
SET password_hash = '240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9'
WHERE username = 'admin';
```
(จะรีเซ็ตรหัสผ่านเป็น "admin123")

### Q8: สามารถใช้งานหลายคนพร้อมกันได้ไหม?
**A:** ได้ แต่ต้อง Deploy บน Cloud (เช่น Streamlit Cloud, Railway) ถ้ารันบนเครื่องใช้ได้แค่คนเดียว

### Q9: ข้อมูลจะหายไหมถ้า Deploy บน Cloud?
**A:** ไม่หาย เพราะเก็บใน Supabase (Database) และ Google Drive (ไฟล์)

### Q10: ต้องเสียเงินไหม?
**A:** ไม่ถ้าใช้:
- Supabase Free Tier (500MB Database)
- Google Drive Free (15GB)
- Streamlit Community Cloud (ฟรี)

---

## 🔧 ส่วนที่ 7: การแก้ปัญหา {#การแก้ปัญหา}

### ปัญหา 1: ไม่สามารถ Login Admin ได้

**วิธีแก้:**
1. ตรวจสอบว่ารัน SQL `setup_database_v2.sql` แล้ว
2. ลอง Login ด้วย:
   - Username: `admin`
   - Password: `admin123`
3. ถ้ายังไม่ได้ รีเซ็ตรหัสผ่านด้วย SQL ในข้อ Q7

### ปัญหา 2: ไม่พบกิจกรรมในหน้า User

**วิธีแก้:**
1. ตรวจสอบว่าสร้างกิจกรรมในหน้า Admin แล้ว
2. Refresh หน้าเว็บ (F5)
3. ตรวจสอบว่าเชื่อมต่อ Supabase ได้

### ปัญหา 3: ไม่สามารถดาวน์โหลดเกียรติบัตรได้

**วิธีแก้:**
1. ตรวจสอบว่าโฟลเดอร์ Google Drive แชร์เป็น "Anyone with the link"
2. ตรวจสอบว่าไฟล์ PDF มีอยู่จริงในโฟลเดอร์
3. ลองคลิกปุ่ม "👁️ ดูไฟล์" แทน

### ปัญหา 4: ระบบช้า

**วิธีแก้:**
- ปิดโปรแกรมอื่นที่ใช้ไม่ได้
- รีสตาร์ทโปรแกรม
- ตรวจสอบอินเทอร์เน็ต

### ปัญหา 5: ข้อมูลไม่ตรงกัน

**วิธีแก้:**
1. ตรวจสอบว่าลำดับใน Excel ตรงกับชื่อไฟล์ PDF
2. ตรวจสอบว่าจำนวนแถวเท่ากับจำนวนไฟล์

---

## 🚀 การ Deploy บน Cloud

ถ้าต้องการให้คนอื่นใช้งานผ่าน Internet:

1. Push โค้ดขึ้น GitHub
2. ไปที่ https://share.streamlit.io
3. Deploy `user_app.py` สำหรับหน้า User
4. Deploy `admin_app.py` สำหรับหน้า Admin (แยกกัน)
5. ตั้งค่า Secrets ใน Streamlit Cloud

**อ่านเพิ่มเติม:** `CLOUD_DEPLOYMENT_GUIDE.md`

---

## 📞 ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
1. อ่านส่วนการแก้ปัญหาด้านบน
2. ตรวจสอบ Error message
3. ตรวจสอบว่าทำตามขั้นตอนครบหรือไม่

---

## ✅ Checklist ก่อนเริ่มใช้งาน

- [ ] ติดตั้ง Python แล้ว
- [ ] ติดตั้ง Dependencies แล้ว
- [ ] สร้าง Supabase Project แล้ว
- [ ] รัน SQL สร้าง Tables แล้ว
- [ ] ตั้งค่า secrets.toml แล้ว
- [ ] แยกไฟล์ PDF เป็นไฟล์เดี่ยวแล้ว
- [ ] อัพโหลดไฟล์ขึ้น Google Drive แล้ว
- [ ] แชร์โฟลเดอร์เป็น Public แล้ว
- [ ] เตรียมไฟล์ Excel รายชื่อแล้ว
- [ ] ทดสอบ Login Admin สำเร็จแล้ว

---

**พร้อมใช้งานแล้ว! สนุกกับระบบเกียรติบัตร V2.0** 🎉

---

**เวอร์ชัน:** 2.0
**อัพเดตล่าสุด:** 2026-01-15
**พัฒนาโดย:** Claude Code + Streamlit + Supabase + Google Drive
