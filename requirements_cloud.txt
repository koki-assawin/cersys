# Requirements for Cloud Version
# Using specific versions that have pre-built wheels for Windows

streamlit>=1.28.0
pandas>=2.0.0
openpyxl>=3.1.0
pypdf>=3.17.0
python-dotenv>=1.0.0

# Supabase dependencies - installed separately to avoid compilation issues
supabase>=2.0.0
